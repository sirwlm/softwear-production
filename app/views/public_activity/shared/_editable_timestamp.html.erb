<% if @current_user.admin? %>
    <%= editable activity, :created_at,
                 url: public_activity_path(activity),
                 title: activity.created_at.strftime('%F %I:%M%P')
    %>
<% else %>
    <%= activity.created_at.strftime('%F %T') %>
<% end %>