try {
  if (window.location.pathname.includes('/orders/')) {
    var onHide = function() {
      <%= render template: 'trains/show.js' %>

      $('#contentModal').off('hidden.bs.modal', onHide);
    };

    $('#contentModal').on('hidden.bs.modal', onHide);
    $('#contentModal').modal('hide');
  }
  else {
    window.location = window.location;
  }
}
catch (e) {
  console.log(e);
}
