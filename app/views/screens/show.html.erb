<%= render 'nav' %>
<h1>Screen #<%= @screen.id %></h1>
<br clear="all" />

<%= render partial: 'view' %>

<div class="col-xs-12">
  <h2>Activity</h2>
  <div id="screen-activities">
    <%= render_activities(@screen.activities.order(created_at: :desc)) %>
  </div>
</div>