$('#imprint-group-<%= @imprint_group.id %>').slideUp();

<% @imprints.each do |imprint| %>
    $('#imprint-<%= imprint.id %>-container').html(
            "<%=j render 'orders/imprint_groups/imprint', imprint: imprint %>"
    );
<% end %>

initializeDraggableImprints();
